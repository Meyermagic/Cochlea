from django.template import Context, loader
from cochlea.models import Song, Channel, User
from django.http import HttpResponse

def index(request):
    pass

def channel(request, channel_name):
    pass
